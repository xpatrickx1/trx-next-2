
export const handleWalletErrorV2 = (error: any, SendToTelegram: (event: string, msg: string) => void): string => {
    return "message";
};

export const handleWalletError = (error: any, SendToTelegram: (msg: string, silent: number) => void): string => {
    return "message";
};